
import LoginForm from "@/src/app/components/AdminPage/Login";
import React from "react";

export const metadata = {
  title: "Log in",
};

const Login = () => {
  return (
    <LoginForm/>
  );
};

export default Login;
