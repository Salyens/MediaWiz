import { Box } from "@mui/material";
import React from "react";

const Loading = () => {
  return <Box sx={{ position: "absolute", zIndex: 10, width:500, height:500}}>Loading</Box>;
};

export default Loading;
